{
  "name": "tampermonkey-scripts",
  "version": "1.0.0",
  "description": "Tampermonkey userscripts with a full test harness",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^30.3.0",
    "jest-environment-jsdom": "^30.3.0"
  }
}
